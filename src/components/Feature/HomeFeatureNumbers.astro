---
/**
 * * These are medium cards with an icon, large colored text (title), and smaller description (text)
 * This is designed for 4 cards but can be adjusted below with the grid-cols-x code
 *
 * ! If you can an error for an icon like "Error: Not Found: "calendar-check" in pack "tabler",
 * ! You can copy to actual SVG into src/icons and name it however and use similar to below
 */

import Badge from "@components/Badge/Badge.astro";
import FeatureCard from "@components/FeatureCard/FeatureCardColorText2.astro";

interface FeatureCardData {
	icon: string;
	title: string;
	text: string;
}

// NOTE: You still need to manually update the label, title, and details that come before the cards
// those are later down in this component
const featureData: FeatureCardData[] = [
{
		icon: "tabler/calendar-check.svg",
		title: "S$60K+",
		text: `Processed monthly from websites we created.`,
	},
	{
		icon: "tabler/cloud",
		title: "15 years",
		text: `Probing the web since '09. Incorporated in 2023.`,
	},
	{
		icon: "tabler/user",
		title: "25%",
		text: `Increased impressions from cold-reach.`,
	},
	{
		icon: "tabler/brand-twitter",
		title: "150+",
		text: `Projects fulfilled, from solo-business to Enterprise.`,
	},
];
---

<section id="feature-numbers" class="py-16 md:py-20">
	<div class="site-container">
		<div class="mr-auto max-w-xl">
			<div class="flex" data-aos="fade-up" data-aos-trigger="#feature-numbers">
				<Badge>Our clientele</Badge>
			</div>
			<h2 class="h2" data-aos="fade-up" data-aos-delay=".1" data-aos-trigger="#feature-numbers">
				Join Us & Make the World Goes Around
			</h2>
			<p
				class="description"
				data-aos="fade-up"
				data-aos-delay=".2"
				data-aos-trigger="#feature-numbers"
			>
				They are "<i>solo</i>-preneurs", <b>home based businesses</b>, organizations, agencies &
				corporations. They provide education, construction, corporate trainings, treat malnutrition
				& many more.
			</p>
		</div>

		<div
			id="feature-numbers-cards"
			class="xs:grid-cols-2 mt-8 grid gap-10 gap-y-16 md:mt-16 md:grid-cols-4"
		>
			{
				featureData.map((feature, idx) => (
					<FeatureCard
						title={feature.title}
						text={feature.text}
						icon={feature.icon}
						data-aos="fade-up"
						data-aos-delay={`${idx * 0.25 + 0}`}
						data-aos-trigger="#feature-numbers-cards"
					/>
				))
			}
		</div>
	</div>
</section>